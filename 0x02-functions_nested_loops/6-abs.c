#include "main.h"
/**
 * _abs - Compute the absolute value of an integer
 *
 * @num: type int
 * 
 * Return: absolute value of @n
 */
int _abs(int num)
{
	if (n < 0)
	{
		return (num * -1);
	}
	else
	{
		return (num);
	}
}
